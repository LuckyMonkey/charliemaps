# Stylized Tiles Placeholder

Put local raster tiles here as `/tiles/{z}/{x}/{y}.png`.

Current frontend behavior:
- Default base layer uses OpenStreetMap.
- "Stylized tiles" toggle switches to `/tiles/{z}/{x}/{y}.png`.

This is the extension point for a future isometric-ish pipeline.
